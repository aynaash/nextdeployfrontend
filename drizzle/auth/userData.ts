// i VG VGmport { db } from "@/lib/db";
// import { users } from "@/lib/schem";
// import { eq } from "drizzle-orm";
//
// export async function getUserById(id: string) {
//   const [user] = await db.select().from(users).where(eq(users.id, id));
//   return user || null;
// }
