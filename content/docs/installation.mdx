---
title: Installation  
description: How to install the NextDeploy CLI and set up your server for deployment.  
---

To get started with NextDeploy, you’ll install the CLI, generate your project config, and connect a remote server (VPS or bare metal) where your Next.js apps will be deployed.

> 💡 NextDeploy is built with Go and works on **Linux, macOS, and Windows**. You can deploy to any server with Docker and SSH access.

---

## 🔧 1. Install the CLI

The CLI is the primary tool to deploy and manage your apps.

### macOS / Linux (with cURL)

```bash
curl -sSL https://nextdeploy.one/install | sh
