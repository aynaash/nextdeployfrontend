# --- Notion content pipeline (docs + guides) ---
# Internal integration secret from https://www.notion.so/my-integrations
NOTION_TOKEN=
# The "Content" database id (share the DB with your integration first)
NOTION_DATABASE_ID=
# Shared secret for POST /api/revalidate (on-demand publish)
REVALIDATE_SECRET=
