version: "1.0"
app:
    name: nextdeploy
    port: 3000
    environment: prod
    domain: nextdeploy.one
    secrets:
        provider: doppler
        project: The name of your project
        config: prod
repository:
    url: git@githuh.com/aynaash/nextdeploy.one
    branch: main
    autoDeploy: true
    webhookSecret: jkadjsdjk34344
docker:
    image: ghcr.io/hersi/nextdeploy
    registry: ghcr.io
    build:
        context: .
        dockerfile: Dockerfile
        noCache: false
    push: true
deployment:
    server:
        host: 90.90.00
        user: hersi
        sshKey: ~/.ssh/id_rsa
        useSudo: true
    container:
        name: hersi
        restart: always
        ports:
            - 80:80
database:
    type: posgres
    host: hersi
    port: 5432
    username: hersi
    password: "503089"
    name: hersi
monitoring:
    enabled: true
    type: grafana
    endpoint: ""
secrets:
    provider: ""
