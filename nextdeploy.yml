version: "1.0"
app:
    name: nextdeploy
    port: 300
    environment: prod
    domain: nextdeploy.one
    secrets:
        provider: aws
        project: nextdeploy
        config: ""
repository:
    url: git@github.com/aynash.git
    branch: main
    autoDeploy: true
docker:
    image: nextdepoy
    build:
        context: .
        dockerfile: Dockerfile
        noCache: true
        args: {}
    push: true
deployment:
    server:
        host: 12.23.23.34
        user: hersi
        sshKey: ~/.ssh/key.rd
        useSudo: false
    container:
        name: nextdeploy
        restart: always
        ports: []
database:
    type: postgres
    host: nextdeploy
    port: "3000"
    username: hersi
    password: "50307813117"
    name: nextdeploy
monitoring:
    enabled: true
    type: grafana
    endpoint: ""
secrets: null
